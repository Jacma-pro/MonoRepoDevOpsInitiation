# 1. Choisir l'image de base (Node.js)
FROM node:18-alpine

# 2. Définir le dossier de travail dans le conteneur
WORKDIR /app

# 3. Copier les fichiers de dépendances (package.json et package-lock.json si présent)
COPY package*.json ./

# 4. Installer les dépendances
RUN npm install

# 5. Copier le reste du code source
COPY . .

# 6. Exposer le port (Vérifiez le port dans src/index.js, disons 3000 par défaut)
EXPOSE 3000

# 7. Commande pour démarrer l'application
CMD ["node", "src/index.js"]